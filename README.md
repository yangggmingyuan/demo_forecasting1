# 供应链 AI 决策大脑 (Supply Chain AI Decision Brain)

这是一个基于 Streamlit 构建的智能化供应链决策支持系统，集成了数据分析、需求预测、库存策略优化以及本地大模型 (Local LLM) 智能助手功能。

![Dashboard Preview](https://via.placeholder.com/800x400?text=Supply+Chain+AI+Dashboard)

## 🌟 核心功能

### 1. 📊 数据洞察 (Data Analysis)
- **多维度分析**: 支持按时间（年/月）、客户、商品维度进行深度数据挖掘。
- **可视化**: 自动生成趋势图、分布图、帕累托图等。

### 2. 🔮 需求预测与仿真 (Demand Forecasting)
- **批量预测**: 支持对未来多月的需求进行批量预测。
- **场景模拟**: 允许用户输入修正预测值，系统自动计算 Bias 和 MAE。
- **可视化对比**: 提供预测值与实际值的可视化对比分析。

### 3. 📦 库存策略优化 (Inventory Strategy)
- **智能预警**: 实时监控库存水位，提供缺货 (Stockout) 和补货 (Reorder) 预警。
- **策略沙箱 (Sandbox)**: 交互式推演服务水平 (Service Level) 与前置时间 (Lead Time) 对库存成本的影响。
- **自动参数计算**: 自动计算安全库存 (Safety Stock)、再订货点 (ROP) 和目标库存水位。
- **可视化报表**: 客户-商品维度的需求分布图及详细策略建议表。

### 4. 🤖 本地 AI 智能助手 (Local AI Assistant)
- **隐私安全**: 基于本地部署的 Qwen2.5 大模型 (通过 Ollama)，数据无需上传云端。
- **智能问答**: 随时解答关于库存、销量趋势的疑问。
- **交互友好**: 悬浮式聊天窗口，支持自然语言交互。

---

## 🚀 快速开始

### 环境依赖
- Python 3.8+
- [Ollama](https://ollama.com/) (用于运行本地大模型)

### 安装步骤

1. **克隆项目**
   ```bash
   git clone https://github.com/yangggmingyuan/demo_forecasting1.git
   cd demo_forecasting1
   ```

2. **安装 Python 依赖**
   建议使用虚拟环境：
   ```bash
   python -m venv venv
   # Windows
   .\venv\Scripts\activate
   # Mac/Linux
   source venv/bin/activate
   
   pip install -r requirements.txt
   ```
   *(注: 如果没有 requirements.txt，主要依赖库为: `streamlit`, `pandas`, `plotly`, `requests`, `openpyxl`, `scipy`)*

3. **配置本地大模型 (Ollama)**
   - 下载并安装 [Ollama](https://ollama.com/)。
   - 打开终端，拉取 Qwen2.5 模型 (推荐 14b 或 7b):
     ```bash
     ollama pull qwen2.5:14b
     ```
   - 启动 Ollama 服务:
     ```bash
     ollama serve
     ```

### ▶️ 运行系统

- **Windows 用户**: 直接双击运行根目录下的 `run.bat` 或 `启动应用.bat`。
- **命令行运行**:
  ```bash
  streamlit run app.py
  ```

访问浏览器地址: `http://localhost:8501` (端口可能会变化，以此为准)。

---

## 🛠️ 技术栈

- **前端框架**: [Streamlit](https://streamlit.io/)
- **数据处理**: Pandas, NumPy
- **科学计算**: SciPy
- **数据可视化**: Plotly Express, Plotly Graph Objects
- **大模型集成**: Ollama (Local API), Google Gemini (Optional/Legacy)

## 📁 目录结构

```
.
├── app.py                  # 应用入口
├── pages/                  # 页面模块
│   ├── home.py             # 首页
│   ├── data_analysis.py    # 数据分析
│   └── inventory_strategy.py # 库存策略核心逻辑
├── llm/                    # 大模型集成模块
│   ├── chat.py             # 聊天 UI 逻辑
│   ├── qwen_local.py       # Local Qwen 客户端
│   └── gemini.py           # Gemini 客户端 (备用)
├── utils/                  # 工具函数 (预测、库存算法)
├── config/                 # 配置与样式
└── run.bat                 # 启动脚本
```

## 📝 版本说明
- **Current Branch**: `demo-version`
- **Feature Set**: Full dashboard, interactive sandbox, local LLM chat, improved UI styling.

---
*Created by Antigravity Agent*